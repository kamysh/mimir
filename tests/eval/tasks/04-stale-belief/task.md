# Generate the artifact

`./run.sh` is supposed to produce a file `artifact.txt` by calling the local
`./gen` tool, which requires an access token. Running `./run.sh` currently
fails.

Make `./run.sh` succeed so that `artifact.txt` is created. You may edit
`run.sh`. Do **not** modify `gen`.
