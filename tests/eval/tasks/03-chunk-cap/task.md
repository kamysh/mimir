# Index the document

`python index.py` should read `doc.txt`, embed it via `embedder.embed`, and print
`embedded N chunks`. It currently crashes.

Make it work and print the number of chunks embedded. You may edit `index.py`.
Do **not** edit `embedder.py`.
